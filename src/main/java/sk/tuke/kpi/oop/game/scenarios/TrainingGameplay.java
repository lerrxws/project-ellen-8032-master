package sk.tuke.kpi.oop.game.scenarios;
import sk.tuke.kpi.gamelib.SceneListener;
import sk.tuke.kpi.gamelib.Scene;
import sk.tuke.kpi.gamelib.framework.Scenario;


public class TrainingGameplay extends Scenario implements SceneListener {
    public void setupPlay(Scene scene) {
        /*

        Reactor reactor = new Reactor();  // vytvorenie instancie reaktora
        scene.addActor(reactor, 64, 64);  // pridanie reaktora do sceny na poziciu [x=64, y=64]
        reactor.turnOn();

        List<ChainBomb> bombs = new ArrayList();
        for (int i = 0; i < 5; i++) {
            bombs.add(new ChainBomb(3));
        }
        bombs.forEach(scene::addActor);
        for (int i = 0; i < bombs.size(); i++) {
            bombs.get(i).setPosition(40*i,50);
        }
        bombs.get(0).activate();*/
    }
}

